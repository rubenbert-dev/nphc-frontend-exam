
/**
 * @todo
 * > Enable CSV upload for User/Employee data
 * > Having fields: id (#employee id), name (#employee name), login (#employee login), salary (#employee salary)
 */
export default async (req, res) => {
    res.status(201).json({ success: true });
}